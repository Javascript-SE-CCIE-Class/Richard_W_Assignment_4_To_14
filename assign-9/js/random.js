
let num = Math.ceil(Math.random() * 1);

// let num = Math.ceil(Math.random() * 10);

let rand = prompt('Take a random integer between 1 to 10 - *hint number set to 1 for practicals* wrx');
if (rand == num)
    alert('Good Work');
else
    alert('Not matched, the number was ' + num);
